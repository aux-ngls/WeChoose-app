{
  "name": "reliure",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "mobile": "cd mobile && npm run dev:auto",
    "mobile:lan": "cd mobile && npm run dev:lan",
    "mobile:update": "cd mobile && npm run update",
    "mobile:typecheck": "cd mobile && npm run typecheck",
    "backend": "cd backend && uvicorn main:app --host 0.0.0.0 --port 8092 --reload",
    "update": "git pull --ff-only && cd mobile && npm install && npm run typecheck"
  }
}
